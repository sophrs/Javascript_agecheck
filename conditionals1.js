let person = {name: "Daniel", age: 22, occupation: "student"};

function checkAge(){
    if (person.age >= 20 && person.age <=40) {
        document.write("true");
    }
    else {
        document.write("false");
    }

}
